<!DOCTYPE html>
<html lang="es" class="bg-gray-50">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Elienor System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['"Manrope"', 'sans-serif'],
                        mono: ['"JetBrains Mono"', 'monospace'],
                    },
                    colors: {
                        gray: {
                            50: '#F9FAFB', 100: '#F3F4F6', 200: '#E5E7EB', 300: '#D1D5DB',
                            400: '#9CA3AF', 500: '#6B7280', 600: '#4B5563', 700: '#374151',
                            800: '#1F2937', 900: '#111827', 950: '#0B0F15',
                        }
                    },
                    boxShadow: {
                        'subtle': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
                        'float': '0 10px 40px -10px rgba(0,0,0,0.08)',
                        'glow': '0 0 15px rgba(0, 0, 0, 0.1)',
                    },
                    animation: {
                        'fade-in': 'fadeIn 0.5s ease-out',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0', transform: 'translateY(10px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' },
                        }
                    }
                }
            }
        }
    </script>
    <style>
        .bg-grid-pattern {
            background-image: radial-gradient(#E5E7EB 1px, transparent 1px);
            background-size: 24px 24px;
        }

        /* Estilos de validación */
        .input-error {
            border-color: #ef4444 !important;
            ring-color: #ef4444 !important;
        }

        .input-success {
            border-color: #22c55e !important;
            ring-color: #22c55e !important;
        }

        .hidden {
            display: none;
        }

        /* Clase para desenfocar el fondo cuando el modal está activo */
        .blur-content {
            filter: blur(8px);
            pointer-events: none;
            user-select: none;
        }
    </style>
</head>

<body class="text-gray-900 font-sans antialiased min-h-screen relative bg-gray-50 bg-grid-pattern overflow-hidden">

    <div id="securityModal"
        class="fixed inset-0 z-50 flex items-center justify-center bg-gray-900/95 backdrop-blur-md transition-all duration-500">
        <div
            class="bg-white rounded-2xl shadow-2xl p-8 w-full max-w-sm mx-4 text-center border border-gray-200 relative overflow-hidden">
            <div class="absolute top-0 left-0 w-full h-1.5 bg-red-600"></div>
            <div class="mb-6 inline-flex p-3 bg-red-50 rounded-full text-red-600">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                    <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                </svg>
            </div>
            <h2 class="text-lg font-bold text-gray-900 mb-2">System Locked</h2>
            <p class="text-xs text-gray-500 mb-6">Security Clearance Level 5 Required.</p>

            <form id="securityForm" onsubmit="unlockSystem(event)">
                <div class="relative mb-4">
                    <input type="password" id="adminToken"
                        class="block w-full px-4 py-3 bg-gray-50 border border-gray-300 rounded-lg text-sm text-center font-mono tracking-widest focus:ring-2 focus:ring-red-500 transition-all"
                        placeholder="MASTER KEY" required autofocus autocomplete="off">
                </div>
                <p id="securityMsg" class="text-[10px] font-bold text-red-500 h-4 mb-2"></p>
                <button type="submit" id="unlockBtn"
                    class="w-full py-3 px-4 bg-gray-900 hover:bg-black text-white rounded-lg text-xs font-bold uppercase tracking-wider transition-colors">Decrypt
                    & Access</button>
            </form>
        </div>
    </div>

    <div id="mainApp"
        class="blur-content transition-all duration-700 min-h-screen flex flex-col items-center justify-center py-12 w-full">

        <div class="absolute inset-0 bg-gradient-to-t from-gray-50 via-transparent to-gray-50 pointer-events-none">
        </div>

        <div class="absolute top-6 right-8 flex items-center gap-3 animate-fade-in z-20">
            <div class="flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200 rounded-full shadow-subtle">
                <span class="relative flex h-2 w-2">
                    <span
                        class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
                </span>
                <span class="text-[10px] font-mono font-medium text-gray-600 tracking-wide uppercase">
                    New Registration
                </span>
            </div>
        </div>

        <main class="w-full max-w-md px-6 z-10 animate-fade-in">
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-12 h-12 bg-gray-900 rounded-xl shadow-glow mb-6">
                    <svg xmlns="http://www.w3.org/2000/svg" width="2em" height="2em" viewBox="0 0 24 24">
                        <path fill="#ffffff"
                            d="M15 12c2.21 0 4-1.79 4-4s-1.79-4-4-4s-4 1.79-4 4s1.79 4 4 4m0-6c1.1 0 2 .9 2 2s-.9 2-2 2s-2-.9-2-2s.9-2 2-2m0 8c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4m6 4H9v-.99c.2-.72 3.3-2.01 6-2.01s5.8 1.29 6 2zM4 9V6H2v3H0v2h2v3h2v-3h3V9z" />
                    </svg>
                </div>
                <h1 class="text-2xl font-bold tracking-tight text-gray-900">Create Account</h1>
                <p class="text-sm text-gray-500 mt-2">Initialize your profile in <span
                        class="font-mono text-gray-700">Elienor.sys</span></p>
            </div>

            <div class="bg-white rounded-2xl shadow-float border border-gray-200 p-8 relative overflow-hidden">
                <div class="absolute top-0 left-0 w-full h-1 bg-blue-600"></div>

                <form id="registerForm" class="space-y-4" action="<%= baseUrl %>/register" method="POST"
                    onsubmit="return validateFinal(event)">
                    <input type="hidden" id="security_token" name="security_token">

                    <div>
                        <label for="name"
                            class="block text-xs font-semibold text-gray-700 uppercase tracking-wide mb-2">Full Identity
                            Name</label>
                        <input type="text" id="name" name="name" required
                            class="block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-lg text-sm text-gray-900 focus:ring-2 focus:ring-gray-900 focus:border-transparent transition-all placeholder-gray-400 font-medium"
                            placeholder="John Doe">
                    </div>

                    <div>
                        <label for="email"
                            class="block text-xs font-semibold text-gray-700 uppercase tracking-wide mb-2">Corporate
                            Email</label>
                        <input type="email" id="email" name="email" required
                            class="block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-lg text-sm text-gray-900 focus:ring-2 focus:ring-gray-900 focus:border-transparent transition-all placeholder-gray-400 font-medium"
                            placeholder="user@elienor.sys">
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">

                        <div>
                            <label for="role"
                                class="block text-xs font-semibold text-gray-700 uppercase tracking-wide mb-2">Access
                                Level</label>
                            <div class="relative">
                                <select id="role" name="role" required
                                    class="appearance-none block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-lg text-sm text-gray-900 focus:ring-2 focus:ring-gray-900 focus:border-transparent transition-all font-medium cursor-pointer placeholder-gray-400">
                                    <option value="" disabled selected>SELECT TIER</option>
                                    <option value="usuario">Usuario Estandar</option>
                                    <option value="editor">Editor</option>
                                    <option value="visual">Viewer</option>
                                    <option value="desarrollador">desarrollador</option>
                                    <option value="demo">Demo Account</option>
                                    <option value="admin">Administrator</option>
                                    <option value="root">Root / System</option>
                                </select>
                                <div
                                    class="pointer-events-none absolute inset-y-0 right-0 flex items-center px-4 text-gray-500">
                                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M8 9l4-4 4 4m0 6l-4 4-4-4" />
                                    </svg>
                                </div>
                            </div>
                        </div>

                        <div>
                            <label for="area"
                                class="block text-xs font-semibold text-gray-700 uppercase tracking-wide mb-2">Area /
                                Zona</label>
                            <div class="relative">
                                <select id="area" name="area"
                                    class="appearance-none block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-lg text-sm text-gray-900 focus:ring-2 focus:ring-gray-900 focus:border-transparent transition-all font-medium cursor-pointer placeholder-gray-400">
                                    <option value="General" selected>General Ops</option>
                                    <option value="IT">Technology (IT)</option>
                                    <option value="RRHH">Human Resources</option>
                                    <option value="Finanzas">Finance & Ledger</option>
                                    <option value="Legal">Legal Compliance</option>
                                    <option value="Ventas">Sales Force</option>
                                    <option value="Marketing">Growth & Mkt</option>
                                </select>
                                <div
                                    class="pointer-events-none absolute inset-y-0 right-0 flex items-center px-4 text-gray-500">
                                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M8 9l4-4 4 4m0 6l-4 4-4-4" />
                                    </svg>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label for="password"
                                class="block text-xs font-semibold text-gray-700 uppercase tracking-wide mb-2">Master
                                Key</label>
                            <div class="relative">
                                <input type="password" id="password" name="password" required
                                    class="block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-lg text-sm text-gray-900 focus:ring-2 focus:ring-gray-900 focus:border-transparent transition-all placeholder-gray-400 font-medium pr-10"
                                    placeholder="••••••••">
                                <button type="button" onclick="togglePass('password')"
                                    class="absolute right-2 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600 p-1">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                    </svg>
                                </button>
                            </div>
                        </div>

                        <div>
                            <label for="confirm_password"
                                class="block text-xs font-semibold text-gray-700 uppercase tracking-wide mb-2">Verify
                                Key</label>
                            <div class="relative">
                                <input type="password" id="confirm_password" name="confirm_password" required
                                    class="block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-lg text-sm text-gray-900 focus:ring-2 focus:ring-gray-900 focus:border-transparent transition-all placeholder-gray-400 font-medium pr-10"
                                    placeholder="••••••••">
                                <button type="button" onclick="togglePass('confirm_password')"
                                    class="absolute right-2 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600 p-1">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                    </svg>
                                </button>
                            </div>
                            <p id="msg" class="text-[10px] mt-1 font-bold hidden uppercase tracking-widest"></p>
                        </div>
                    </div>

                    <div class="flex items-center gap-2 py-2">
                        <input type="checkbox" id="terms"
                            class="rounded border-gray-300 text-gray-900 focus:ring-gray-900">
                        <label for="terms" class="text-xs text-gray-500">I accept the <a href="#"
                                class="text-gray-900 underline">System Protocols</a></label>
                    </div>

                    <button type="submit" id="submitBtn"
                        class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-md text-sm font-bold text-white bg-gray-900 hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-900 transition-all transform active:scale-[0.98] disabled:opacity-50 disabled:cursor-not-allowed">
                        Initialize Onboarding
                    </button>
                </form>

                <div class="mt-6 text-center">
                    <p class="text-sm text-gray-500">
                        Already registered?
                        <a href="/login" class="font-bold text-gray-900 hover:underline">Access Terminal</a>
                    </p>
                </div>
            </div>

            <div class="mt-8 text-center">
                <p class="text-xs text-gray-400 font-medium tracking-widest uppercase">Encryption: AES-256 Enabled</p>
            </div>
        </main>

        <div class="absolute bottom-6 left-8 opacity-50 hidden lg:block">
            <div class="text-[10px] font-mono text-gray-400 space-y-1">
                <p>REG_ZONE: CDMX-NORTH-02</p>
                <p>AUTH_TYPE: OAUTH2 / JWT</p>
            </div>
        </div>
    </div>

    <script>
        window.SERVER_DATA = {
            baseUrl: "<%= baseUrl %>"
        };
    </script>

    <script src="/js/register_funciones.js"></script>
    <script src="/js/tools.js"></script>


</body>

</html>